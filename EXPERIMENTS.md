# 小行星雷达回波自转周期测量实验计划

> 状态：已批准，实施中  
> 计划版本：v0.4  
> 依据：Calvés 等（2025）的连续波雷达、微多普勒分析和层析处理思路  
> 本阶段范围：仅定义实验方案、软件边界、实验矩阵和验收标准；审批前不进入实现
>
> v0.2 变更：明确采用三角多面体作为统一几何表示；确定“椭球验证后再上真实小行星模型”的递进路线；配置格式改为 JSON + JSON Schema；当前使用连续波，同时预留非均匀多时刻脉冲序列；加入 Astropy 星历模块与 NeuS_radar 复用审查。

> 2026-07-29 实施记录：已完成首版 M1/M2 基线。严格 JSON 配置、椭球网格、CW 复回波、显式时间戳、平动多普勒补偿、STFT、谱特征和 Lomb–Scargle 候选均已运行；8 项自动测试通过。6 h 基线中真实周期为 7200 s，RMS 带宽粗估计为 7154.92 s（相对误差 0.626%）。该结果属于快速基线验证，不替代 E2 正式蒙特卡洛实验。

> 2026-07-30 实施记录：几何模块迁移为 PyTorch3D `Meshes` 与 `ico_sphere`，姿态旋转和面元复回波迁移到 Torch；新增 OBJ/PLY 入口、CPU `float64` 参考路径、CUDA `float32` 加速路径及后端一致性测试。11 项测试通过。采用 1280 面元的 CUDA 基线运行 5.42 s，较原 528 面元 NumPy 基线的 26.44 s 加速约 4.88 倍；RMS 带宽粗估计为 7148.71 s（相对误差 0.712%）。

> 2026-07-30 光行时扩展：新增独立三事件解算器，使用 JPL Horizons 的太阳系质心几何状态和 Astropy 地面站状态，迭代求解发射、反射、接收时刻，并分别输出发射与接收视线。该工具尚未并入连续波回波主链路。完整变更记录见 [EXPERIMENT_LOG.md](EXPERIMENT_LOG.md)。

> 2026-07-30 v0.3：按科研流程重构为 `pointing`、`simulation`、`inversion` 和 `data` 四个模块。删除严格 JSON Schema 与重复输入防御逻辑；仿真和反演通过 `EchoDataset` 文件连接，可分别运行和验证。只保留会直接影响科研结论的检查。

> 2026-07-30 v0.4：进一步改为扁平源码。形状生成与回波仿真完全分离；仿真配置只指定标准 OBJ/PLY 的 `model_path`。椭球先由独立脚本生成 OBJ，真实小行星模型可以直接替换路径。配置、脚本和测试不再重复建立阶段子目录。

## 1. 项目目标

建立一套可复现的小行星雷达回波仿真与处理系统，从含噪复基带回波中精确估计小行星的自转周期，并定量评估估计精度对目标形状、散射特性、观测几何、信噪比、观测时长、轨道多普勒残差和数据缺失的敏感性。

项目的核心科学问题是：

1. 小行星旋转如何在连续波雷达回波中形成周期性的微多普勒和回波功率调制？
2. 在整体平动多普勒远大于旋转微多普勒时，如何稳定提取旋转信息？
3. 如何处理对称目标造成的半周期混叠，以及低信噪比、短观测弧段造成的不确定性？
4. 在已知或近似已知目标形状的条件下，物理模型驱动的精细反演能将周期精度提高到什么程度？

## 2. 范围与边界

### 2.1 本项目包含

1. 连续波雷达照射下的小行星复基带回波仿真。
2. 目标整体平动多普勒与自转微多普勒的联合建模。
3. 多面元散射、热噪声、相位噪声、轨道补偿误差和观测缺口建模。
4. 回波预处理、平动多普勒估计与补偿、时频分析和特征提取。
5. 自转周期的粗估计、精细反演、置信区间计算和质量控制。
6. 参数扫描、蒙特卡洛实验、消融实验和可重复性记录。
7. 为后续非均匀多时刻脉冲回波预留采集时刻与波形 seam。

### 2.2 本阶段不包含

1. 真实射电望远镜或深空网数据接入。
2. 完整轨道确定、测距码设计和真实天线控制。
3. 非主轴自由翻滚的完整动力学反演；该项仅作为扩展实验评估模型失配。
4. 以神经网络作为主周期估计器。
5. 将微多普勒反演结果宣称为真实光学形状图像。

## 3. 关键假设与可辨识性

### 3.1 基线假设

1. 发射信号为单频连续波，默认载频为 7.15 GHz，以便与参考论文保持可比。
2. 基线采用单发单收双基地几何；收发同址的单基地模式作为对照，多接收站作为扩展。
3. 小行星在基线实验中绕固定主轴匀速旋转。
4. 仿真提供名义轨道径向速度或低阶多普勒先验，但允许加入残差。
5. 所有实体表面统一转换为三角多面体；基线网格由三轴椭球离散得到，验证通过后换成真实小行星三维网格。
6. 点云不作为主几何表示；点云输入必须先重建为带法向和面片面积的表面网格。离散点散射模型只用于解析验证或专门的散射中心实验。

### 3.2 必须显式处理的不可辨识问题

均匀球体绕自身旋转时，几何外形和均匀散射分布不随时间变化，通常不能从回波中辨识自转周期。因此：

1. 均匀球体只作为负对照，不作为周期测量基线。
2. 可测目标必须至少具备非轴对称形状、表面散射不均匀或离散强散射中心之一。
3. 对称三轴椭球可能每半圈产生近似重复回波，使估计器输出 \(P/2\)。实验必须同时报告基频与二次谐波，并通过不规则形状、幅度信息或多视角观测判别真实周期。
4. 当观测时长不足一个完整周期时，只允许报告“弱约束估计”或区间，不得输出伪精确点估计。

## 4. 总体技术路线

```text
目标形状、散射参数、自转状态、观测几何
                  ↓
        多面元连续波回波仿真
                  ↓
      复基带 I/Q + 真值 + 元数据
                  ↓
  质量检查、去直流、滤波、数据缺口标记
                  ↓
   整体多普勒跟踪与相位旋转补偿
                  ↓
 STFT/动态谱、回波功率、频谱质心和带宽
                  ↓
 周期图 + 自相关 + 谐波一致性粗估计
                  ↓
 物理前向模型驱动的局部精细反演
                  ↓
 周期估计、置信区间、混叠标记、质量评分
```

## 5. 模块一：小行星回波仿真

### 5.1 目标几何模型

#### 5.1.1 表示选择

回波生成器内部统一使用带三角面片的封闭或近似封闭多面体，而不直接使用点云。原因是面元回波计算需要稳定的面法向、面积、入射角、散射角、可见性和遮挡关系；普通点云缺少这些量，结果会显著依赖采样密度，难以进行物理标定。

点云只保留两种用途：

1. 作为外部输入格式，经过法向估计和表面重建后转换为三角网格；
2. 作为“离散强散射中心”模型，用于验证多普勒公式和研究局部强散射，不代表完整实体表面。

回波模块只接收标准化后的 `SurfaceMesh`，调用方不需要知道其来源是解析椭球、OBJ/PLY 文件还是点云重建。

#### 5.1.2 递进验证路线

不直接从真实模型开始，采用三层递进：

1. **解析验证目标**
   - 单散射点；
   - 双散射点；
   - 均匀球体负对照；
2. **椭球多面体验证目标**
   - 由三轴椭球解析生成三角面元网格；
   - 可控制长短轴比、尺度和旋转轴方向；
   - 可在表面加入固定的强散射斑块。
3. **真实小行星三维模型**
   - 支持 OBJ、PLY 和 STL 三角网格；
   - 导入时进行单位、尺度、坐标轴、质心、法向、闭合性和退化面检查；
   - 必要时做简化或重网格，但保留原始模型及转换记录；
   - 第一候选目标可使用 2008 EV5，与 NeuS_radar 中已有参数和多时刻数据保持关联；
   - 真实模型实验在椭球模型通过 E0、E1 后开展，避免将坐标、相位或遮挡错误误判为真实形状效应。

每个目标文件必须同时保存：

- 网格顶点与面元；
- 物理尺度；
- 质心和主惯量轴；
- 面元法向；
- 面元散射系数；
- 生成随机种子。

### 5.2 运动与观测几何

基线采用固定自转轴和恒定周期：

\[
\theta(t)=\theta_0+\frac{2\pi t}{P}.
\]

目标姿态由四元数更新，面元位置和速度由刚体旋转计算。观测几何至少包含：

- 发射机位置和速度；
- 接收机位置和速度；
- 小行星质心位置和速度；
- 自转轴在惯性坐标系中的方向；
- 初始相位；
- 双基地夹角和视线方向。

首轮回波主链路不要求高保真太阳系轨道积分。目标整体距离变化用可控的距离多项式或径向速度多项式表示，确保能够独立评估多普勒补偿误差。独立工具已经实现真实星历与双程光行时解算，后续在完成精度验证后整合到回波主链路。

### 5.3 星历、时间与坐标系统

Astropy 用于：

- UTC、TT、TDB 等时间尺度转换；
- J2000/ICRS、黄道坐标、地固系和地平坐标转换；
- 地面站位置与观测时刻管理；
- 保持单位信息，避免角度、距离和速度单位混用。

需要注意：Astropy 本身主要负责时间和坐标基础设施，并不直接提供任意小天体的完整在线精密星历。真实小行星星历建议通过 `astroquery.jplhorizons` 获取 JPL Horizons 结果，再用 Astropy 的 `Time`、`SkyCoord`、`EarthLocation` 和单位系统进行转换；离线复现时把已下载星历固化为本地文件，不让正式实验依赖实时网络。

时间计算不直接把“距 J2000 的全部秒数”以 `float32` 保存。内部保留 Astropy `Time` 或高精度双部分儒略日，姿态相位计算使用相对当前观测段参考时刻的 `float64` 秒数，降低长时间跨度下的精度损失。

### 5.4 面元回波模型

发射复包络记为 \(s(t)\)。第 \(i\) 个可见面元的双基地传播时延为

\[
\tau_i(t)=\frac{R_{\mathrm{T},i}(t)+R_{i,\mathrm{R}}(t)}{c},
\]

对应多普勒频率为

\[
f_{\mathrm{D},i}(t)
=-\frac{1}{\lambda}
\frac{\mathrm{d}}{\mathrm{d}t}
\left[R_{\mathrm{T},i}(t)+R_{i,\mathrm{R}}(t)\right].
\]

下变频后的复基带回波建模为

\[
y(t)=
\sum_{i\in\mathcal{V}(t)}
A_i(t)
\exp\left[-j2\pi f_c\tau_i(t)+j\phi_i\right]
+n(t),
\]

其中 \(\mathcal{V}(t)\) 是同时满足发射可见和接收可见条件的面元集合，\(A_i(t)\) 综合自由空间传播损耗、面元面积、入射角、散射角和材料散射系数，\(n(t)\) 为复高斯噪声。

首轮采用可解释的余弦幂散射模型：

\[
A_i(t)\propto
\rho_i S_i
\max(0,\mathbf{n}_i\cdot\mathbf{u}_{\mathrm{T}})^{p}
\max(0,\mathbf{n}_i\cdot\mathbf{u}_{\mathrm{R}})^{q}.
\]

实现层面保留散射模型接口，以便后续替换为 Lambert、镜面/漫反射混合或更高保真电磁模型。

### 5.5 信号与采样配置

建议基线配置如下，最终数值在实现前固化到 JSON 配置：

| 参数 | 基线值 | 说明 |
|---|---:|---|
| 载频 | 7.15 GHz | 与参考论文一致 |
| 小行星等效直径 | 100 m | 后续扫描 20～500 m |
| 真实自转周期 | 2 h | 后续扫描 0.5～12 h |
| 观测时长 | 6 h | 覆盖 3 个周期 |
| 复基带采样率 | 64 Hz | 应高于最大旋转微多普勒的两倍并留余量 |
| STFT 窗长 | 4096 点 | 初始值，需通过分辨率实验调整 |
| STFT 重叠率 | 75% | 平衡时间与频率分辨率 |
| 基线信噪比 | 10 dB | 另扫描 -10～30 dB |

仿真开始前必须自动估计最大可能微多普勒并检查奈奎斯特条件。若配置可能混叠，程序应拒绝运行或自动提高采样率，不得静默产生错误数据。

### 5.6 简洁 JSON 配置

可调实验参数继续保存在 JSON 中，但不再使用 JSON Schema、配置对象和逐字段
防御性校验。配置保持为三个平坦文件：

- `configs/pointing.json`：星历、测站和观测时间；
- `configs/echo.json`：模型路径、目标运动、雷达、采样和噪声；
- `configs/inversion.json`：STFT和周期搜索参数。

参数名显式写出单位。研究脚本直接通过 `json.loads` 读取字典，让配置值到
公式变量的对应关系保持可见。仅在错误会静默破坏科研结论时检查，例如采样
率导致多普勒混叠、照射几何产生零回波或光行时迭代不收敛。

批量参数扫描以短脚本复制并修改基础字典，不引入配置框架。

### 5.7 连续波与未来脉冲序列的 seam

当前实验只实现连续波，但数据模型从第一版就使用显式观测时刻，不把“固定 PRF”写入核心接口。

统一采集接口返回：

- `epoch_times`：每个观测历元的精确时刻；
- `fast_time`：单次连续窗口或单个脉冲内部的快时间；
- `iq`：对应的复回波；
- `coherence_id`：标识哪些样本之间保证相位相干；
- `valid_mask`：观测缺口；
- 波形与几何元数据。

连续波 adapter 产生一个或多个连续相干时间窗。未来脉冲 adapter 接受任意严格递增的 `pulse_times_utc`，因此脉冲间隔可以不均匀，整个序列跨度可以超过一个自转周期。

未来脉冲处理分两种情况：

1. 同一相干脉冲簇内部保留复相位，进行距离压缩和多普勒处理；
2. 相隔很远或不保证相干的脉冲簇之间，不直接拼接载波相位，而是提取每个历元的功率、带宽、轮廓或距离-多普勒特征，再使用真实时间戳进行 Lomb–Scargle 和物理模型反演。

这样可以避免对非均匀脉冲序列做错误的等间隔补零 FFT。

### 5.8 非理想因素

按以下顺序逐步加入：

1. 复高斯热噪声；
2. 剩余频率偏置和低阶多普勒漂移；
3. 相位噪声与频率标准不稳定；
4. 幅度闪烁；
5. 不规则采样和观测缺口；
6. 自转角速度缓慢变化；
7. 非主轴翻滚，用于测试模型失配。

### 5.9 仿真输出

每次运行输出：

- `echo_iq`：复基带 I/Q；
- `time`：时间轴；
- `truth`：真实周期、自转轴、初始相位、目标尺度；
- `geometry`：收发站与目标几何；
- `config`：完整 JSON 配置和随机种子；
- `components`：可选的逐面元或逐散射中心贡献；
- `diagnostics`：真实平动多普勒、真实旋转微多普勒范围、瞬时信噪比。

大规模数组使用 HDF5 或 Zarr；汇总指标使用 CSV/Parquet；配置使用 JSON。

## 6. 模块二：回波信号处理

### 6.1 数据质量检查

处理前检查：

1. 时间戳单调性和采样间隔；
2. 缺失样本与异常脉冲；
3. I/Q 饱和、直流偏置和幅相不平衡；
4. 载波是否位于可处理带宽内；
5. 估计信噪比及有效观测时长；
6. 理论微多普勒带宽是否发生采样混叠。

### 6.2 整体多普勒估计与补偿

实现三种与参考论文相对应的模式：

1. **真值补偿**：使用仿真真值，作为算法上限。
2. **先验补偿**：使用加入误差的轨道多普勒先验。
3. **数据驱动补偿**：从回波频谱脊线估计低阶多项式或平滑轨迹。

补偿通过复基带相位旋转完成：

\[
y_{\mathrm{c}}(t)=y(t)
\exp\left[-j2\pi\int_0^t \widehat{f}_{\mathrm{trans}}(\xi)\mathrm{d}\xi\right].
\]

必须保留补偿前后动态谱，用于检查旋转微多普勒是否被过拟合的平滑轨迹一并消除。

### 6.3 时频分析

主方法使用 STFT 生成动态谱，输出：

- 频谱峰值轨迹；
- 频谱质心；
- 有效多普勒上、下包络；
- 90% 能量带宽或稳健分位数带宽；
- 总回波功率；
- 若干强散射脊线。

STFT 窗长不能固定套用到所有周期。系统需根据预期微多普勒带宽和周期提供默认建议，并在实验中扫描窗长，量化时频分辨率权衡。

### 6.4 周期相关特征

至少构建三条一维时间序列：

1. 回波总功率 \(P_{\mathrm{echo}}(t)\)；
2. 微多普勒带宽 \(B_{\mathrm{D}}(t)\)；
3. 动态谱相似度或谱图主成分系数 \(z(t)\)。

同时保留完整二维动态谱供模板匹配使用。任何单一特征失效时，不应直接判定整个观测失败。

## 7. 模块三：自转参数反演

### 7.1 粗周期估计

并行使用三类经典估计器：

1. 广义 Lomb–Scargle 周期图：适合带缺口或不规则采样；
2. 自相关函数：寻找回波形态重复时延；
3. 谐波一致性评分：联合基频、二次和三次谐波，识别 \(P/2\) 混叠。

三个估计器输出候选周期列表而不是只输出单个峰值。候选集至少包含最高峰、两倍周期和半周期，并记录峰值显著性。

### 7.2 多特征融合

对功率、带宽和谱图相似度三个通道分别计算候选周期，再以信噪比、峰值显著性和通道一致性加权融合。若不同通道分别支持 \(P\) 和 \(P/2\)，输出混叠标记并进入物理模型精细反演。

### 7.3 物理模型驱动的精细反演

以粗估计候选为初值，在局部范围内优化：

- 自转周期 \(P\)；
- 初始相位 \(\theta_0\)；
- 自转轴两个方向角；
- 总幅度和噪声尺度；
- 可选的低阶残余频率漂移。

损失函数由动态谱差异和一维特征差异组成：

\[
\mathcal{L}
=w_1 d_{\mathrm{spec}}
+w_2 d_{\mathrm{power}}
+w_3 d_{\mathrm{band}}
+w_4 \mathcal{R},
\]

其中 \(d_{\mathrm{spec}}\) 使用归一化动态谱相关损失，避免绝对功率标定误差主导结果；\(\mathcal{R}\) 为参数先验正则项。

第一版采用粗网格加局部无梯度优化，优先保证稳定和可解释。只有在性能确实受限时，才考虑可微仿真或贝叶斯采样。

### 7.4 不确定性与质量控制

输出不能只有一个周期数值，还必须包含：

- 点估计；
- 置信区间；
- 候选别名周期；
- 周期图峰值显著性；
- 不同特征的一致性；
- 是否覆盖至少一个、两个或三个完整周期；
- 是否存在 \(P/2\) 混叠；
- 质量等级：可靠、可疑、不可辨识。

置信区间首选分块自助法或噪声重采样蒙特卡洛。若计算开销过大，可先使用局部目标函数曲率给出近似区间，但必须在报告中标注。

## 8. 实验设计

### 8.1 E0：解析正确性验证

目的：确认多普勒符号、尺度、坐标变换和相位累积正确。

用例：

1. 静止单散射点：补偿后频率应为零。
2. 匀速单散射点：频率应与解析多普勒一致。
3. 匀速圆周运动散射点：瞬时频率应呈解析正弦变化。
4. 均匀球体：不得产生可稳定识别的形状旋转周期。
5. 无噪声双散射点：估计周期及其谐波应与真值对应。

### 8.2 E1：无噪声端到端基线

目标：证明从多面元回波到周期估计的完整链路正确。

条件：

- 三轴椭球加一个非均匀散射区；
- 观测 3 个周期；
- 使用真值平动多普勒补偿；
- 无噪声、无缺口。

比较粗估计和精细反演结果，并保存 I/Q、动态谱、特征曲线、周期图和损失函数局部剖面。

### 8.3 E2：信噪比蒙特卡洛

信噪比取：

\[
\{-10,-5,0,5,10,15,20,30\}\ \mathrm{dB}.
\]

每个信噪比至少运行 100 个随机种子，报告：

- 周期绝对误差和相对误差；
- 中位数、95% 分位误差；
- 成功率；
- \(P/2\) 混叠率；
- 置信区间覆盖率；
- 运行时间。

### 8.4 E3：观测时长实验

观测时长取：

\[
\{0.5P,1P,1.5P,2P,3P,5P\}.
\]

目的：确定可靠估计至少需要覆盖多少个周期。小于 \(P\) 的结果主要用于验证“不确定性应增大”，不要求达到高精度。

### 8.5 E4：形状与散射复杂度

比较：

1. 均匀球体；
2. 均匀三轴椭球；
3. 带散射斑块的三轴椭球；
4. 真实小行星三角网格；
5. 真实网格的不同简化等级；
6. 真实网格加离散强散射中心。

目的：量化目标非对称性对可辨识性和半周期混叠的影响，并确认周期估计对网格分辨率不过度敏感。

### 8.6 E5：观测几何

扫描：

- 自转轴与视线夹角；
- 双基地夹角；
- 初始旋转相位；
- 单基地与双基地模式；
- 可选的双接收站模式。

重点识别自转轴接近视线方向时微多普勒变弱的退化区域。

### 8.7 E6：平动多普勒补偿误差

比较真值补偿、带误差先验补偿和数据驱动补偿。注入：

- 常量频偏；
- 线性频漂；
- 二阶频漂；
- 先验系数误差。

检查过度拟合是否削弱真实旋转微多普勒。

### 8.8 E7：缺口和非理想因素

分别测试：

- 随机丢样；
- 连续观测中断；
- 幅度闪烁；
- 相位噪声；
- 少量脉冲干扰。

比较普通 FFT 周期图与 Lomb–Scargle、自相关方法的鲁棒性。

### 8.9 E8：模型失配

使用一种模型产生数据、另一种模型反演：

- 不规则目标生成，椭球模型反演；
- 混合散射生成，余弦幂模型反演；
- 缓慢变速或非主轴翻滚生成，恒定主轴模型反演。

目的不是保证全部通过，而是明确适用边界，并检验质量控制能否将错误的高置信度结果拒绝掉。

### 8.10 E9：算法消融

至少比较：

1. 仅回波功率；
2. 仅微多普勒带宽；
3. 仅完整动态谱；
4. 功率、带宽和谱图联合；
5. 联合特征加物理模型精细反演；
6. 不做平动多普勒补偿；
7. 不做谐波混叠处理。

## 9. 评价指标与验收标准

### 9.1 主要指标

\[
\epsilon_P=\frac{|\widehat{P}-P|}{P}.
\]

同时报告：

- 周期绝对误差；
- 周期相对误差；
- 成功率；
- \(P/2\)、\(2P\) 混叠率；
- 95% 置信区间覆盖率；
- 失败检测率与错误高置信度率；
- 单次运行耗时和峰值内存。

### 9.2 分级验收

#### A. 数学与软件正确性

1. E0 中解析多普勒用例的数值误差满足离散采样和浮点精度预期。
2. 配置、真值、随机种子和输出之间可以完整追溯。
3. CPU 与 CUDA 路径在容差内产生一致结果。
4. 自动测试覆盖坐标变换、相位累积、FFT 频率轴、周期混叠判定和配置校验。

#### B. 无噪声基线

1. E1 粗估计必须将真值周期或其明确标记的谐波纳入候选集。
2. 精细反演周期相对误差不超过 0.1%。
3. 均匀球体必须被判定为不可辨识，不得输出虚假的高置信度周期。

#### C. 含噪基线

在 SNR 不低于 10 dB、观测时长不低于 \(3P\)、基线不规则目标条件下：

1. 100 次蒙特卡洛实验的周期相对误差中位数不超过 0.5%；
2. 95% 分位相对误差不超过 2%；
3. 成功率不低于 95%；
4. 错误高置信度率不超过 1%；
5. 95% 置信区间的经验覆盖率目标为 90%～98%。

上述阈值是工程验收目标，不预先宣称一定能够达到。若未达到，必须保留完整失败案例并说明限制，不能用快速验证替代正式蒙特卡洛结果。

#### D. 鲁棒性报告

E3～E9 不设置统一通过阈值，其验收要求是：

1. 完成预定参数网格；
2. 报告误差曲线、成功率和混叠率；
3. 明确给出可靠工作区、退化区和不可辨识区；
4. 所有结论可由保存的汇总数据重新绘图验证。

## 10. 软件结构

当前结构：

```text
rotation-period-measurement/
├── README.md
├── README_EN.md
├── EXPERIMENTS.md
├── EXPERIMENT_LOG.md
├── environment.yml
├── models/
│   └── ellipsoid.obj
├── configs/
│   ├── echo.json
│   ├── inversion.json
│   └── pointing.json
├── src/
│   └── asteroid_radar/
│       ├── ellipsoid.py
│       ├── mesh.py
│       ├── motion.py
│       ├── echo.py
│       ├── dataset.py
│       ├── signal.py
│       ├── inversion.py
│       ├── ephemeris.py
│       └── pointing.py
├── scripts/
│   ├── make_ellipsoid.py
│   ├── simulate_echo.py
│   ├── estimate_period.py
│   └── solve_pointing.py
├── tests/
│   ├── test_mesh.py
│   ├── test_echo.py
│   ├── test_inversion.py
│   ├── test_00_pointing.py
│   └── test_z_backend.py
├── docs/
├── outputs/
└── notebooks/
```

OBJ/PLY是模型准备与回波仿真之间的seam；`EchoDataset`是仿真与反演之间的
seam。命令行脚本只负责读取配置、保存数据和绘图。

## 11. 科研代码与复现要求

1. Python 命令统一通过 `conda run -n pytorch` 执行。
2. 有实际加速收益时使用 `cuda:0`，同时保留 CPU 回退。
3. 代码优先展示公式和数据流，不为正常研究流程中不会出现的输入增加重复校验。
4. 只保留会影响科学结论的检查，例如混叠、零信号、时间顺序和迭代不收敛。
5. 所有实验由普通 JSON 配置驱动，保存配置、环境、随机种子和运行耗时。
6. 仿真与反演分别运行，通过 `EchoDataset` 连接。
7. 正式结果与临时文件不纳入 Git；`.gitignore` 忽略输出和缓存。
8. 提供中文与英文 README。
9. 快速冒烟测试与正式蒙特卡洛实验分别标记。
10. 图中同时标注真值、估计值和置信区间，不只展示成功案例。

## 12. 预期交付物

审批并实施后交付：

1. 可配置的小行星复基带回波仿真器；
2. 多普勒补偿、动态谱和特征提取程序；
3. 粗周期估计、物理模型精细反演和不确定性评估程序；
4. 单元测试和端到端测试；
5. E0～E9 实验配置与运行脚本；
6. 机器可读的实验汇总数据；
7. 动态谱、周期图、误差曲线、成功率和消融对比图；
8. 中文和英文使用说明；
9. 最终实验报告，明确区分已验证结果、失败结果和适用边界。

## 13. 建议实施里程碑

### M1：物理模型与解析验证

- 完成简洁JSON配置、坐标、Astropy时间、刚体旋转、单散射点和椭球多面元回波；
- 完成 E0；
- 交付解析对照图和测试结果。

### M2：信号处理基线

- 完成平动多普勒补偿、STFT 和特征提取；
- 完成无噪声端到端链路；
- 交付 E1。

### M3：周期反演

- 完成周期图、自相关、谐波一致性和多特征融合；
- 完成局部物理模型精细反演；
- 交付 E2、E3。

### M4：鲁棒性与失配

- 完成 E4～E9；
- 接入至少一个真实小行星三维网格并完成网格分辨率对比；
- 完成置信区间、失败检测和消融分析；
- 形成适用边界。

### M5：复现与交付

- 完成文档、双语 README、配置固化和最终报告；
- 在干净环境中重新运行代表性实验；
- 核对所有图表能由保存数据重建。

## 14. NeuS_radar 代码复用审查

已检查 `D:\Downloads\NeuS_radar`。结论是“选择性借鉴并重写验证”，不整体复制。

### 14.1 可借鉴内容

1. `functions/Asteroid.py`
   - 已封装小行星直径、自转周期、角速度和极轴；
   - 使用 Astropy 在黄道、赤道和 ICRF 表示之间转换；
   - 可借鉴参数组织方式。
2. `models/dataset.py`
   - `calculate_seconds_from_j2000` 展示了 Astropy `Time` 的矢量化时间转换；
   - 数据按真实 `timestamps` 和 `los_icrf` 组织，适合借鉴为多历元观测记录；
   - `get_los_bft` 给出了 ICRF 视线到旋转本体系的基本思路。
3. `functions/RotateVectors.py`
   - 包含 Rodrigues 批量旋转和参考系变换；
   - 可作为解析测试的对照实现。
4. `public_data/2008_EV5_radar`
   - 已有 2008 EV5 的多时刻数据组织示例；
   - 每组包含 ICRF 视线、UTC 时间戳、距离/多普勒分辨率和波长；
   - 现有样例以 300 s 等间隔保存，未来的新接口应推广到任意非均匀时刻。

### 14.2 不能直接复用或必须修正的内容

1. 项目内没有连续波或脉冲复回波生成代码；`check_results.py` 把雷达图像生成委托给外部 `roais`，该包当前未安装在 `pytorch` 环境。
2. `radarSet.py` 使用单基地 \(2v/\lambda\) 公式并假设固定 PRF，不适用于双基地和非均匀脉冲时刻，只能借鉴参数命名。
3. 多处使用 `float32` 保存极轴、旋转和长时间偏移；本项目的高精度周期测量应使用 `float64`，时间保留 Astropy 高精度表示。
4. `geocentrictrueecliptic` 未显式指定历元；真实小行星极轴必须按数据源明确使用 ICRS、J2000 平黄道或其他具体坐标框架，不能依赖默认值。
5. Rodrigues 变换的方向、正负号和平行/反平行退化情况需要重新写解析测试，未经测试不直接复制。
6. NeuS_radar 目标是从多时刻雷达图像重建形状，而本项目首先从复回波估计周期；其神经渲染主流程不属于首轮复用范围。

### 14.3 复用方式

不在源文件上直接依赖 NeuS_radar。将可用概念重新实现为本项目内部的深模块，并通过解析测试验证：

- `EphemerisModule`：输入目标、站点和时刻，输出统一参考系下的位置、速度和视线；
- `ShapeModule`：输入解析参数或网格文件，输出标准 `SurfaceMesh`；
- `AcquisitionModule`：输入显式时刻和波形配置，输出统一观测批次；
- `EchoModule`：只依赖上述标准结果，不了解 Astropy、OBJ/PLY 或连续波/脉冲的内部细节。

若复制任何受 MIT 许可证保护的具体代码片段，保留原版权和许可证说明。

## 15. 已确定的方案

1. 是否批准以三角多面体作为统一实体几何表示，点云仅作为输入或离散散射中心？
2. 是否批准“解析目标 → 椭球网格 → 真实小行星网格”的验证顺序？
3. 是否批准当前以 7.15 GHz 连续波为基线，并按第 5.7 节预留非均匀脉冲序列？
4. 可调参数使用按研究阶段拆分的普通 JSON，不使用严格 Schema。
5. 是否批准 Astropy 负责时间与坐标、JPL Horizons 数据负责真实小行星星历的方案？
6. 是否同意第一阶段使用固定主轴匀速旋转，把非主轴翻滚放入模型失配实验？
7. 是否同意基线周期取 2 h、观测时长取 6 h，并在参数扫描中覆盖 0.5～12 h？
8. 是否同意首轮采用可解释的余弦幂面元散射模型，而不直接开展高成本全波电磁仿真？
9. 是否同意以“粗周期候选 + 物理模型局部反演”为主算法，并将机器学习排除在核心验收之外？
10. 是否接受第 9 节的精度、成功率与置信区间验收目标？

以上方案已经进入实施；后续改变核心科学假设时更新本文件版本。
